{
  "name": "amazon-project",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.1.1",
    "@vitest/coverage-c8": "^3.1.1"
  }
}
